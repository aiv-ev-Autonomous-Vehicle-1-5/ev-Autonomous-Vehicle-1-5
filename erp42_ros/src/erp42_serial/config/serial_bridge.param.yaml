# serial_bridge.param.yaml

serial_bridge:
  ros__parameters:

    # Odometry frame id
    odometry_frame_id      : "odom"
    odometry_child_frame_id: "base_footprint"

    # Serial port settings , baud_rate는 건들지 말기, 차량 연결된 포트에 맞춰 port_path 수정
    port_path: "/dev/ttyUSB0"
    baud_rate: 115200 

    # ERP42 parameters // 웬만하면 건들지 말기. 차량 기계공학적으로 한계인 값들임.
    max_speed_mps      :  7.00 # m/s
    max_steering_deg   : 25.00 # deg
    steering_offset_deg:  0.00 # deg