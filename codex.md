# Codex Workspace Notes

Write repository-specific instructions for future Codex sessions here.

Suggested sections:

- Project overview
- Build and test commands
- Code style rules
- Files or directories to avoid changing
- Preferred workflow for ROS 2 packages
- Review checklist

Example:

- Use `colcon build --packages-select <pkg>` for package-local validation before broader builds.
- Prefer minimal edits and avoid unrelated refactors.
- Do not change generated files unless the task explicitly requires it.
